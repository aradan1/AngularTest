{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jwt-simple": "^0.5.6"
  },
  "name": "ejemplo-server",
  "version": "1.0.0",
  "description": "API REST on an Express server ",
  "main": "index.ts",
  "scripts": {
    "server": "nodemon -L",
    "client": "npm run start --prefix ./client",
    "start": "npm run server && npm run client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yo",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.13.0"
  }
}
